(function(){
  var seriousFlags=['刺痛','麻木','放射痛','关节卡住','明显无力'];
  function avg(o){var v=Object.values(o||{});return v.length?Math.round(v.reduce(function(a,b){return a+b},0)/v.length*10)/10:0}
  function records(){return Object.values((window.data&&data.records)||{}).sort(function(a,b){return a.date.localeCompare(b.date)})}
  function lastRecords(n){var r=records();return r.slice(Math.max(0,r.length-n))}
  function hasSerious(r){return !!(r&&(r.flags||[]).some(function(f){return seriousFlags.indexOf(f)>=0}))}
  function highPainTwice(){var r=lastRecords(2);return r.length>=2&&r.every(function(x){return avg(x.after)>=6||avg(x.before)>=6})}
  function insertAlert(){var old=document.querySelector('.health-alert');if(old)old.remove();var r=lastRecords(1)[0];if(!r)return;var msg='';if(hasSerious(r))msg='已记录刺痛、麻木、放射痛、关节卡住或明显无力等异常信号。建议暂停训练，观察变化；若持续、加重或影响日常活动，请及时就医。';else if(highPainTwice())msg='最近连续2次记录疼痛评分达到6分或以上。建议暂停加量，降低训练强度；若持续不缓解，建议咨询医生或康复治疗师。';if(!msg)return;var div=document.createElement('div');div.className='health-alert';div.textContent=msg;var hero=document.querySelector('.hero');if(hero)hero.parentNode.insertBefore(div,hero)}
  function patchedCoach(){var el=document.getElementById('coach');if(!el)return;var r=lastRecords(1)[0];if(!r){el.className='warning';el.innerHTML='保存记录后，这里会根据疼痛评分和异常信号给出保守建议。<div class="coach-note">提示仅用于训练记录管理，不能替代诊断。</div>';return}var b=avg(r.before),a=avg(r.after);if(hasSerious(r)){el.className='danger';el.innerHTML='建议暂停训练：你记录了异常信号。若刺痛、麻木、放射痛、明显无力或关节卡住持续存在，请及时就医。<div class="coach-note">此建议不能替代医生诊断。</div>'}else if(highPainTwice()){el.className='danger';el.innerHTML='建议就医/咨询康复专业人士：最近连续2次疼痛评分达到6分或以上。先不要进阶或加量。<div class="coach-note">此建议仅基于记录阈值，不能替代诊断。</div>'}else if(a-b>=2||a>=6){el.className='danger';el.innerHTML='建议降级：本次训练后评分升高较明显，或训练后评分较高。下次减量，避免硬练。<div class="coach-note">若持续加重，请及时就医。</div>'}else if(a<=b&&a<=4){el.className='warning ok';el.innerHTML='方向较稳：训练后不适没有增加，可以继续当前阶段；连续2次稳定后再小幅加量。<div class="coach-note">仍以不耸肩、不憋气、不腰顶为前提。</div>'}else{el.className='warning';el.innerHTML='建议维持：暂时不要加量，优先把动作做稳，观察第二天反应。<div class="coach-note">若出现异常信号，应停止训练并考虑就医。</div>'}}
  function drawTrend(){var holder=document.getElementById('painTrendBox');if(!holder)return;var r=records().slice(-14);if(r.length<2){holder.innerHTML='<div class="trend-empty">至少保存2条训练记录后，这里会显示训练前/训练后疼痛评分趋势。</div>';return}holder.innerHTML='<canvas class="trend-canvas" id="painTrendCanvas"></canvas>';var c=document.getElementById('painTrendCanvas'),dpr=window.devicePixelRatio||1,w=holder.clientWidth-24,h=holder.clientHeight-24;c.width=w*dpr;c.height=h*dpr;c.style.width=w+'px';c.style.height=h+'px';var ctx=c.getContext('2d');ctx.scale(dpr,dpr);var pad={l:34,r:12,t:14,b:34},cw=w-pad.l-pad.r,ch=h-pad.t-pad.b;function x(i){return pad.l+(r.length===1?0:i*cw/(r.length-1))}function y(v){return pad.t+ch-(v/10)*ch}ctx.clearRect(0,0,w,h);ctx.lineWidth=1;ctx.strokeStyle='rgba(148,163,184,.35)';ctx.fillStyle='rgba(100,116,139,.9)';ctx.font='12px -apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif';[0,2,4,6,8,10].forEach(function(v){ctx.beginPath();ctx.moveTo(pad.l,y(v));ctx.lineTo(w-pad.r,y(v));ctx.stroke();ctx.fillText(v,pad.l-26,y(v)+4)});function line(key,color){ctx.beginPath();r.forEach(function(o,i){var v=avg(o[key]);if(i)ctx.lineTo(x(i),y(v));else ctx.moveTo(x(i),y(v))});ctx.lineWidth=3;ctx.strokeStyle=color;ctx.stroke();r.forEach(function(o,i){ctx.beginPath();ctx.arc(x(i),y(avg(o[key])),3.5,0,Math.PI*2);ctx.fillStyle=color;ctx.fill()})}line('before','#2563eb');line('after','#dc2626');ctx.fillStyle='rgba(100,116,139,.95)';r.forEach(function(o,i){if(i%Math.ceil(r.length/5)===0||i===r.length-1)ctx.fillText(o.date.slice(5),x(i)-14,h-10)});ctx.fillStyle='#2563eb';ctx.fillText('训练前',pad.l,14);ctx.fillStyle='#dc2626';ctx.fillText('训练后',pad.l+58,14)}
  function addTrend(){var report=document.getElementById('report');if(!report||document.getElementById('painTrendCard'))return;var card=document.createElement('div');card.className='trend-card';card.id='painTrendCard';card.innerHTML='<h3>疼痛趋势图表</h3><p class="sub">基于历史记录显示训练前/训练后平均疼痛评分，纵轴0–10分。</p><div class="trend-wrap" id="painTrendBox"></div>';var cal=document.getElementById('calendar');if(cal&&cal.parentNode)cal.parentNode.insertBefore(card,cal.parentNode.firstChild);else report.appendChild(card);setTimeout(drawTrend,50)}
  function beep(){try{var A=window.AudioContext||window.webkitAudioContext;if(!A)return;var a=new A(),o=a.createOscillator(),g=a.createGain();o.connect(g);g.connect(a.destination);o.frequency.value=880;g.gain.value=.06;o.start();setTimeout(function(){o.stop();a.close()},260)}catch(e){}}
  function notifyTimer(){if(navigator.vibrate)navigator.vibrate([180,80,180]);beep()}
  var oldTimerStart=window.timerStart;window.timerStart=function(type){oldTimerStart(type);if(type==='rest'){var t=timers.rest;if(t&&t.id&&!t._wrapped){var id=t.id;t._wrapped=true;clearInterval(id);t.id=setInterval(function(){t.seconds=Math.max(0,t.seconds-1);var el=document.getElementById('restTime');if(el)el.textContent=fmt(t.seconds);if(t.seconds===0){t.on=false;clearInterval(t.id);t._wrapped=false;notifyTimer()}},1000)}}};
  var oldSave=window.saveRecord;window.saveRecord=function(){oldSave();setTimeout(function(){patchedCoach();insertAlert();addTrend();drawTrend()},80)};
  var oldClear=window.clearData;window.clearData=function(){var text='再次确认：清空后，本设备里的所有康复训练记录会删除。建议先导出JSON备份。请输入“清空”继续。';if(prompt(text)==='清空')oldClear()};
  var oldReport=window.renderReport;window.renderReport=function(){oldReport();addTrend();drawTrend()};
  window.showGuide=function(){var old=document.querySelector('.guide-backdrop');if(old)old.remove();var b=document.createElement('div');b.className='guide-backdrop';b.innerHTML='<div class="guide-modal"><h2>训练前先看这几条</h2><p>这个网页只用于记录和辅助判断，不能替代医生诊断。</p><ul><li>进阶：连续2次训练不加重、动作不耸肩不憋气，再小幅加量。</li><li>降级：训练后评分增加2分以上、第二天明显更酸、或疼痛超过48小时。</li><li>暂停/就医：刺痛、麻木、放射痛、关节卡住、明显无力，或评分连续2次≥6。</li></ul><div class="guide-actions"><button class="btn primary" id="guideOk">我知道了</button><button class="btn" id="guideLater">下次再看</button></div></div>';document.body.appendChild(b);document.getElementById('guideOk').onclick=function(){localStorage.setItem('rehabGuideSeen','1');b.remove()};document.getElementById('guideLater').onclick=function(){b.remove()}};
  function addGuideButton(){var help=document.getElementById('help');if(help&&!document.getElementById('guideAgain')){var btn=document.createElement('button');btn.id='guideAgain';btn.className='btn primary';btn.textContent='重新查看进阶/降级规则';btn.onclick=window.showGuide;help.insertBefore(btn,help.children[1]||null)}}
  setTimeout(function(){patchedCoach();insertAlert();addTrend();addGuideButton();if(!localStorage.getItem('rehabGuideSeen'))window.showGuide()},600);
})();